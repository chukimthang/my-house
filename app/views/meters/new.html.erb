<div class="container-fluid px-5">
  <h1 class="my-4">Nhập chỉ số</h1>
  <div class="mb-4">
    <%= form_for @meter, url: { action: 'create' }, html: { class: 'form-horizontal' } do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>
